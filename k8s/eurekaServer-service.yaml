apiVersion: v1
kind: Service
metadata:
  name: eurekaServer-service
spec:
  selector:
    app: eurekaServer
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 9090
      nodePort: 30001
  type: NodePort
